/*
function todos() {
  let todos = document.querySelector(".user");
  for(let i = 0; i < todos.length; i++) {
    todos[i].classList.add(mostra);
    todos[i].classList.remove(esconde);
  }
}

function user() {
  let todos = document.querySelector(".user");
  for(let i = 0; i < todos.length; i++) {
    if(todos[i].admCheck.checked == true) {
      todos[i].classList.add(esconde);
      todos[i].classList.remove(mostra);
    }
    else {
      todos[i].classList.add(mostra);
      todos[i].classList.remove(esconde);
    }
  }
}

function admi() {
  let todos = document.querySelector(".user");
  for(let i = 0; i < todos.length; i++) {
    if(todos[i].admCheck.checked == true) {
      todos[i].classList.add(mostra);
      todos[i].classList.remove(esconde);
    }
    else {
      todos[i].classList.add(esconde);
      todos[i].classList.remove(mostra);
    }
  }
}
*/
